<!DOCTYPE html>
<html lang="en">

<%- include("../includes/head.ejs") %>

<body>
  <% if (users.length> 0) { %>
  <ul>
    <% users.forEach((user) => { %>
    <li>
      <%= user.name %>
    </li>
    <% }) %>
  </ul>
  <% } else { %>
  <h1>No Users found!</h1>
  <% } %>
</body>

</html>